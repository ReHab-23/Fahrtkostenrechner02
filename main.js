/*
JavaScript-Code hier
Funktionen etc.
*/

/*
PYSCRIPT-CODE

            def fahrkostenrechnung():
                    kraftstoff = Element('kraftstoff').value
                    x = float(kraftstoff)
                    verbrauch = Element('verbrauch').value
                    y = float(verbrauch)
                    strecke = Element('strecke').value
                    z = float(strecke)

                    verbrauchskosten = ((x * y) / 100) * z
                    Element('output1').write(f"{verbrauchskosten}")
*/

function zuruecksetzen() {
    //zurücksetzen der Eingabefelder etc.
    ergebnis = " ";
    document.getElementById('output').innerText = ergebnis;
    //weitere Eingabefelder zurücksetzen
}

function berechnen() {
    //Werte aus HTML-Dokument holen
    //Kosten berechnen
    //Ergebnis ausgeben
}